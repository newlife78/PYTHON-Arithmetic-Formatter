Install the following packages:
    . pytest: ' pip install -U pytest '

    . NOTE: if it returns an error try to check if pip, setuptools, and wheel are up to date:
            ' py -m pip install --upgrade pip setuptools wheel '


Program Description:
It solves a simple mathematical problem.

First the user enters the problem information, then the program will check if the given data is valid,
if it isn’t it will send a message to the user, if it is valid data it returns the data of the problem
and the solution in an intuitive graphical from.